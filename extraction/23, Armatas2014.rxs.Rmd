---
title: "Systematic Review Extraction Script Template"
date: "26 Jun 2018 at 16:07:01"
output:
  html_document:
    self-contained: yes
    toc: false
params:
  rxsVersion = "0.1.0"
---

```{r setup, include=FALSE}
### First load (and perhaps install) userfriendlyscience
if (!require('userfriendlyscience')) {
  install.packages('userfriendlyscience');
  require('userfriendlyscience');
}

### Other packages
safeRequire('googlesheets');     ### To import data from google sheets in metabefor
safeRequire('jsonlite');         ### To import a list of country codes in metabefor
safeRequire('data.tree');        ### To work with data structured in a tree in metabefor
safeRequire('pander');           ### To print the dataframe with results in a nice way
safeRequire('devtools');         ### To install metabefor from github repo
                                 ### ... Which we then do here:
devtools::install_github('Matherion/metabefor', quiet=TRUE);
require('metabefor');            ### ... After which we load it

### Settings
knitr::opts_chunk$set(echo = FALSE);             ### Suppress R command printing
knitr::opts_chunk$set(comment=NA);               ### Suppress output prefix
pander::panderOptions('table.split.table', Inf); ### Disable table splitting
```

```{r fieldname-chunk}
eC <- list(titleCol="title",
           descriptionCol="description",
           identifierCol="identifier",
           valueTemplateCol="valueTemplate",
           validValuesCol="validValues",
           defaultCol="default",
           parentCol="parent",
           entityRefCol="entityRef",
           fieldRefCol="fieldRef",
           ownerCol="owner",
           listCol="list",
           collapsingCol="collapsing",
           repeatingCol="repeating",
           recursingCol="recursing");

valueTemplateCols <- list(identifierCol="identifier",
                          descriptionCol="description",
                          defaultCol="default",
                          examplesCol="examples",
                          validationCol="validation",
                          errorCol="error");
```

```{r rxsChunk, echo=FALSE}
################################################################################
######################################################## START: study (ROOT) ###
################################################################################
study <- Node$new('study');
################################################################################

  ##############################################################################
  ########################################################### START: studyId ###
  ##############################################################################
  study$AddChild('studyId');
  study$studyId[['value']] <-
  ##############################################################################
  ### 
  ### STUDY IDENTIFIER
  ### 
  ### Unique identifier for this study
  ### 
  ##############################################################################
      
      "10.1080/17461391.2012.736537"
      
  ##############################################################################
  ########################################### VALUE DESCRIPTION AND EXAMPLES ###
  ##############################################################################
  ### 
  ### For studies with a DOI, "doi:" directly followed by the DOI; for
  ### studies without a DOI, the DOI prefix of the publisher (see
  ### https://gist.github.com/TomDemeranville/8699224), followed by a
  ### slash, followed by the ISSN, followed by a dash, followed by the
  ### year of publication, followed by a dash, followed by the starting
  ### page. If no DOI prefix can be located, instead use 'NA'.
  ### 
  ### EXAMPLES:
  ### 
  ### "doi:10.18637/jss.v036.i03"
  ### "nodoi:10.1111/0965-2140/2003-951"
  ### "doi:10.1136/bmj.2.2288.1243"
  ### 
  ##############################################################################
  study$studyId[['validation']] <- expression(nchar(VALUE)>10);
  ##############################################################################
  ############################################################# END: studyId ###
  ##############################################################################


  ##############################################################################
  ########################################################## START: comments ###
  ##############################################################################
  study$AddChild('comments');
  study$comments[['value']] <-
  ##############################################################################
  ### 
  ### GENERAL COMMENTS
  ### 
  ### Any general comments the coder wants to add about this study
  ### 
  ##############################################################################
      
      ""
      
  ##############################################################################
  ########################################### VALUE DESCRIPTION AND EXAMPLES ###
  ##############################################################################
  ### 
  ### A single character value
  ### 
  ### EXAMPLES:
  ### 
  ### "Example"
  ### "Another example"
  ### 
  ##############################################################################
  study$comments[['validation']] <- expression(is.character(VALUE) && length(VALUE) == 1);
  ##############################################################################
  ############################################################ END: comments ###
  ##############################################################################


  ##############################################################################
  ############################################################## START: year ###
  ##############################################################################
  study$AddChild('year');
  study$year[['value']] <-
  ##############################################################################
  ### 
  ### YEAR
  ### 
  ### Year (or years) the data were collected
  ### 
  ##############################################################################
      
      NA
      
  ##############################################################################
  ########################################### VALUE DESCRIPTION AND EXAMPLES ###
  ##############################################################################
  ### 
  ### A numeric vector of years
  ### 
  ### EXAMPLES:
  ### 
  ### 2001
  ### c(2001, 2002)
  ### 
  ##############################################################################
  study$year[['validation']] <- expression(is.numeric(VALUE) && all(nchar(VALUE)==4));
  ##############################################################################
  ################################################################ END: year ###
  ##############################################################################


  ##############################################################################
  ########################################################### START: country ###
  ##############################################################################
  study$AddChild('country');
  study$country[['value']] <-
  ##############################################################################
  ### 
  ### COUNTRY
  ### 
  ### Country (or countries) where the data were collected
  ### 
  ##############################################################################
      
      NA
      
  ##############################################################################
  ########################################### VALUE DESCRIPTION AND EXAMPLES ###
  ##############################################################################
  ### 
  ### A character vector of the ISO 3166-1 alpha-2 country code(s)
  ### 
  ### EXAMPLES:
  ### 
  ### "NL"
  ### c("NL", "BE")
  ### 
  ##############################################################################
  study$country[['validation']] <- expression(VALUE %in% countryCodeList());
  ##############################################################################
  ############################################################# END: country ###
  ##############################################################################


  ##############################################################################
  ########################################################### START: methods ###
  ##############################################################################
  study$AddChild('methods');
  ##############################################################################
  ### 
  ### METHODS
  ### 
  ### Description of the methods used by the study
  ### 
  ##############################################################################
      
  
    ############################################################################
    ############################################################### START: N ###
    ############################################################################
    study$methods$AddChild('N');
    study$methods$N[['value']] <-
    ############################################################################
    ### 
    ### SAMPLE SIZE
    ### 
    ### Total number of human participants in the study (note: the
    ### actual sample size may be larger if multiple observations are
    ### collected per participant)
    ### 
    ############################################################################
        
        NA
        
    ############################################################################
    ######################################### VALUE DESCRIPTION AND EXAMPLES ###
    ############################################################################
    ### 
    ### Any valid whole number
    ### 
    ### EXAMPLES:
    ### 
    ### 30
    ### 8762
    ### 
    ############################################################################
    study$methods$N[['validation']] <- expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1));
    ############################################################################
    ################################################################# END: N ###
    ############################################################################
  
  
    ############################################################################
    ########################################################### START: Ntype ###
    ############################################################################
    study$methods$AddChild('Ntype');
    study$methods$Ntype[['value']] <-
    ############################################################################
    ### 
    ### SAMPLE SIZE TYPE
    ### 
    ### Whether the sample size counts the number of persons or the
    ### number of matches
    ### 
    ############################################################################
        
        NA
        
    ############################################################################
    ######################################### VALUE DESCRIPTION AND EXAMPLES ###
    ############################################################################
    ### 
    ### A string that has to exactly match one of the values specified
    ### in the "values" column of the Coding sheet
    ### 
    ### EXAMPLES:
    ### 
    ### "person"
    ### "match"
    ### 
    ############################################################################
    study$methods$Ntype[['validation']] <- expression(VALUE %in% c("person", "match"));
    ############################################################################
    ############################################################# END: Ntype ###
    ############################################################################
  
  
    ############################################################################
    ########################################### START: subsample (REPEATING) ###
    ############################################################################
    study$methods$AddChild('subsample__1__');
    study$methods$subsample__1__[['value']] <-
    ############################################################################
    ### 
    ### SUBSAMPLE
    ### 
    ### If studies contain several samples, specify them here
    ### 
    ############################################################################
        
        list(subsample.name = "season",  ### Name: A unique name (in this study), to be able to refer to this subsample later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
             subsample.N = 17,                                ### Size: Sample size of subsample [Examples: 30; 8762] [Value description: Any valid whole number]
             subsample.Ntype = "season");                           ### Sample size type: Whether the sample size counts the number of persons or the number of matches [Examples: "person"; "match"; "season"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
        
    ############################################################################
    study$methods$subsample__1__[['validation']] <- list(`subsample.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                         `subsample.N` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                         `subsample.Ntype` = expression(VALUE %in% c("person", "match", "season")));
    study$methods$subsample__1__$name <- study$methods$subsample__1__$value[[1]];
    ############################################################################
    ############################################# END: subsample (REPEATING) ###
    ############################################################################
  
  
    ############################################################################
    ###################################################### START: methodType ###
    ############################################################################
    study$methods$AddChild('methodType');
    study$methods$methodType[['value']] <-
    ############################################################################
    ### 
    ### TYPE OF STUDY
    ### 
    ### Whether the study was qualitative or quantitative
    ### 
    ############################################################################
        
        "quantitative"
        
    ############################################################################
    ######################################### VALUE DESCRIPTION AND EXAMPLES ###
    ############################################################################
    ### 
    ### A string that has to exactly match one of the values specified
    ### in the "values" column of the Coding sheet
    ### 
    ### EXAMPLES:
    ### 
    ### "qualitative"
    ### "quantitative"
    ### "mixed methods"
    ### 
    ############################################################################
    study$methods$methodType[['validation']] <- expression(VALUE %in% c("qualitative", "quantitative", "mixed methods"));
    ############################################################################
    ######################################################## END: methodType ###
    ############################################################################
  
  
    ############################################################################
    ############################################# START: operationalisations ###
    ############################################################################
    study$methods$AddChild('operationalisations');
    ############################################################################
    ### 
    ### OPERATIONALISATIONS
    ### 
    ### The operationalisations (measurements or manipulations) used in
    ### the study
    ### 
    ############################################################################
        
    
      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "gameLocationIsHome",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "logical",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################
    
       

      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "cardsToFoulRatio",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "totalShots",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "clearanceFromPenArea",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "headedShots",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "cornerKicks",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "saves",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################



      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "kickedShotsInsidePenArea",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "kickedShotsOutsidePenArea",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "goals",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "yellowCards",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "foulsCommitted",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################



      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "penaltyKicks",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "redCards",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "steals",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "offsides",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "goalDiff",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


      ##########################################################################
      ########################################## START: variable (REPEATING) ###
      ##########################################################################
      study$methods$operationalisations$AddChild('variable__1__');
      study$methods$operationalisations$variable__1__[['value']] <-
      ##########################################################################
      ### 
      ### VARIABLE
      ### 
      ### A variable and its operationalisation
      ### 
      ##########################################################################
          
          list(oper.name = "teamAbility",  ### Variable Name: A unique name (in this study), to be able to refer to this variable later, and to easily find it afterwards [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
               oper.moment = 1,                            ### Moment: Moment(s) this variable was measured/manipulated. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.type = NA,                             ### Type of operationalisation: Whether this variable is a manipulation, a single measurement, or an aggregate [Examples: "manipulation"; "item"; "aggregate"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.datatype = "numeric",                         ### Type of data: "Measurement level" of this operationalisation. [Examples: "numeric"; "logical"; "nominal"; "ordinal"; "string"] [Value description: A string that has to exactly match one of the values specified in the "values" column of the Coding sheet]
               oper.values = NA,                           ### Values: Possible values this variable can take: only valid for "nominal" or "ordinal" variables. [Examples: c(23, 62); 52; c(76, 12, 42)] [Value description: A vector of integers (i.e. one or more whole numbers)]
               oper.labels = NA,                           ### Labels: Labels for the values. [Examples: c("First value", "Second value")] [Value description: A character vector (i.e. one or more strings)]
               oper.description = "",                      ### Description: A description of this variable (can be more extensive than the name) [Examples: "Example"; "Another example"] [Value description: A single character value]
               oper.comment = "");                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
          
      ##########################################################################
      study$methods$operationalisations$variable__1__[['validation']] <- list(`oper.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                                              `oper.moment` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.type` = expression(VALUE %in% c("manipulation", "item", "aggregate")),
                                                                              `oper.datatype` = expression(VALUE %in% c("numeric", "logical", "nominal", "ordinal", "string")),
                                                                              `oper.values` = expression(is.numeric(VALUE) && all(VALUE%%1==0)),
                                                                              `oper.labels` = expression(is.character(VALUE)),
                                                                              `oper.description` = expression(is.character(VALUE) && length(VALUE) == 1),
                                                                              `oper.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
      study$methods$operationalisations$variable__1__$name <- study$methods$operationalisations$variable__1__$value[[1]];
      ##########################################################################
      ############################################ END: variable (REPEATING) ###
      ##########################################################################


    ############################################################################
    ############################################### END: operationalisations ###
    ############################################################################
  
      
  ##############################################################################
  ############################################################# END: methods ###
  ##############################################################################


  ##############################################################################
  ########################################################### START: results ###
  ##############################################################################
  study$AddChild('results');
  ##############################################################################
  ### 
  ### RESULTS
  ### 
  ### Results based on the data collected in this study
  ### 
  ##############################################################################
      
  
    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_totalShots",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "totalShots",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .47,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################
  

    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_clearanceFromPenArea",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "clearanceFromPenArea",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .46,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_headedShots",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "headedShots",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .39,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################
  

    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_totalShots",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "cornerKicks",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .36,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################



    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_saves",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "saves",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .36,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_KickedShotsInsidePenArea",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "kickedShotsInsidePenArea",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .33,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_kickedShotsOutsidePenArea",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocation",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "kickedShotsOutsidePenArea",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .31,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_goals",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "goals",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .28,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################



    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_yellowCards",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "yellowCards",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .24,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_foulsCommitted",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "foulsCommitted",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .22,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################



    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_penKicks",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "penKicks",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .14,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_redCards",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "redCards",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .13,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_steals",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "steals",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .09,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_offsides",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "offsides",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .02,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "shotsInsidePenArea_goalDiff",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "shotsInsidePenArea",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "goalDiff",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = .488,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = NA,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "shotsOutsidePenArea_goalDiff",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "shotsOutsidePenArea",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "goalDiff",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = .03,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = NA,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "headedShots_goalDiff",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "headedShots",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "goalDiff",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = .123,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = NA,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "teamAbility_goalDiff",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "shotsInsidePenArea",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "goalDiff",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = .506,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = NA,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ######################################### START: association (REPEATING) ###
    ############################################################################
    study$results$AddChild('association__1__');
    study$results$association__1__[['value']] <-
    ############################################################################
    ### 
    ### ASSOCIATION
    ### 
    ### An association between two variables.
    ### 
    ############################################################################
        
        list(assoc.name = "gameLocationIsHome_cardsToFoulRatio",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "mainHypothesisTest"; "manipulationCheck"] [Value description: A single character value that cannot be omitted]
             assoc.var1name = "gameLocationIsHome",     ### First variable: The name of the first variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var1values = NA,                       ### Values of first variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var1moment = NA,                       ### Measurement moment of first variable: To which measurement moment of the first variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2name = "cardsToFoulRatio",     ### Second variable: The name of the second variable. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             assoc.var2values = NA,                       ### Values of second variable: Values this variable pertains to (e.g. in post hoc analyses). [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.var2moment = NA,                       ### Measurement moment of second variable: To which measurement moment of the second variable this association pertains. [Examples: <no example>] [Value description: A string that specifies another field in another entity (can be missing, i.e. NA).]
             assoc.subsample = NA,                        ### Subsample: If applicable, the subsample this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity (can be missing, i.e. NA)]
             assoc.n = NA,                                ### Sample size: This can be used to specify the sample size, if it is different from the sample size of the entire sample or the relevant subsample. [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.df = NA,                               ### Degrees of freedom: This can be used to specify the degrees of freedom for this analysis, in case for example a t or an F value is extracted. [Examples: c(23.43, 62); 52.2; c(76, 12.56, 42)] [Value description: A vector of valid numbers]
             assoc.t = NA,                                ### t value: The t value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.F = NA,                                ### F value: The F value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.r = NA,                                ### Pearson r: The bivariate correlation of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.z = NA,                                ### Z value: The Z value of this association. [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.chisq = NA,                            ### Chi square: The chi squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.crosstab = NA,                         ### Raw table: A table with the frequencies [Examples: rawTable(10, 5, 15, 10); rawTable(31, 87, 21, 54, 25, 32, ncol=3)] [Value description: A table with frequencies; variable 1 in columns, variable 2 in rows; always work from absence/negative/less (left, top) to presence/positive/more (right, bottom)]
             assoc.d = .13,                                ### Cohen's d: The Cohen's d value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.etasq = NA,                            ### Eta squared: The eta squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.omegasq = NA,                          ### Omega squared: The omega squared value of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.OR = NA,                               ### Odds Ratio: The odds ratio of this association [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.p = NA,                                ### p value: The association's p value (only extract if nothing else is available) [Examples: 2.3; 643.2] [Value description: Any valid number]
             assoc.covariates = NA,                       ### Covariates: Number of covariates (for multivariate analyses). [Examples: 30; 8762] [Value description: Any valid whole number]
             assoc.comment = NA);                         ### Comment: Any relevant comments the coder wants to add [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$association__1__[['validation']] <- list(`assoc.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                           `assoc.var1name` = expression(TRUE),
                                                           `assoc.var1values` = expression(TRUE),
                                                           `assoc.var1moment` = expression(TRUE),
                                                           `assoc.var2name` = expression(TRUE),
                                                           `assoc.var2values` = expression(TRUE),
                                                           `assoc.var2moment` = expression(TRUE),
                                                           `assoc.subsample` = expression(TRUE),
                                                           `assoc.n` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.df` = expression(is.numeric(VALUE)),
                                                           `assoc.t` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.F` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.r` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.z` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.chisq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.crosstab` = expression(is.na(VALUE) || class(VALUE) == "matrix"),
                                                           `assoc.d` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.etasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.omegasq` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.OR` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.p` = expression(is.numeric(VALUE) && (length(VALUE) == 1)),
                                                           `assoc.covariates` = expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1)),
                                                           `assoc.comment` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$association__1__$name <- study$results$association__1__$value[[1]];
    ############################################################################
    ########################################### END: association (REPEATING) ###
    ############################################################################


    ############################################################################
    ########################################## START: univariate (REPEATING) ###
    ############################################################################
    study$results$AddChild('univariate__1__');
    study$results$univariate__1__[['value']] <-
    ############################################################################
    ### 
    ### UNIVARIATE
    ### 
    ### A univariate result, e.g. a quote in a qualitative study
    ### 
    ############################################################################
        
        list(uni.name = "Enter name here (mandatory)",  ### Name of result: A unique name (in this study), to easily find / meta-analyse / group this result later [Examples: "Example"; "Another example"] [Value description: A single character value that cannot be omitted]
             uni.varname = "enter  here",               ### Name of variable: The variable this result pertains to. [Examples: <no example>] [Value description: A string that specifies another entity and which MUST be provided]
             uni.qualitative = NA);                     ### Qualitative observation: A quote of a participant in a qualitative study or an observation by the authors in a qualitative study. [Examples: "Example"; "Another example"] [Value description: A single character value]
        
    ############################################################################
    study$results$univariate__1__[['validation']] <- list(`uni.name` = expression(!is.na(VALUE) && !is.null(VALUE) && (nchar(VALUE) > 0)),
                                                          `uni.varname` = expression(TRUE),
                                                          `uni.qualitative` = expression(is.character(VALUE) && length(VALUE) == 1));
    study$results$univariate__1__$name <- study$results$univariate__1__$value[[1]];
    ############################################################################
    ############################################ END: univariate (REPEATING) ###
    ############################################################################
  
      
  ##############################################################################
  ############################################################# END: results ###
  ##############################################################################


  ##############################################################################
  #################################################### START: assoc.nMatches ###
  ##############################################################################
  study$AddChild('assoc.nMatches');
  study$assoc.nMatches[['value']] <-
  ##############################################################################
  ### 
  ### NUMBER OF MATCHES
  ### 
  ### The number of matches to which the association pertains.
  ### 
  ##############################################################################
      
      NA
      
  ##############################################################################
  ########################################### VALUE DESCRIPTION AND EXAMPLES ###
  ##############################################################################
  ### 
  ### Any valid whole number
  ### 
  ### EXAMPLES:
  ### 
  ### 30
  ### 8762
  ### 
  ##############################################################################
  study$assoc.nMatches[['validation']] <- expression(is.numeric(VALUE) && (VALUE%%1==0) && (length(VALUE) == 1));
  ##############################################################################
  ###################################################### END: assoc.nMatches ###
  ##############################################################################

    
################################################################################
########################################################## END: study (ROOT) ###
################################################################################
class(study) <- c('rxs', class(study));
```

```{r show-extracted-data-chunk, results='asis'}
print.rxs(study);
```

```{r validation-chunk, results='asis'}
rxs_validation(study);
cat0("\n\n### Validation results\n\n");
#rxs_validation(study,
#               rxsStructure = fullResults$rxsStructure);
if (length(study$validationResults) > 2) {
  cat(paste0('- ', study$validationResults), sep='
');
} else {
  cat('Validation successful!');
}
```

